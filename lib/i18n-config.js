export const defaultLocale = 'en';
export const supportedLocales = ['en', 'nl', 'en-US', 'nl-NL'];

// Normalized short codes (used for URL paths and locale stripping)
export const knownLocales = ['en', 'nl'];
